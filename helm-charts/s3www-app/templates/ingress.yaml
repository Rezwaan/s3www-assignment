{{- if .Values.s3www.ingress.enabled -}}
{{- $fullName := include "s3www-app.fullname" . -}}
{{- $svcPort := .Values.s3www.service.port -}}
{{- if and .Values.s3www.ingress.className (not (hasKey .Values.s3www.ingress.annotations "kubernetes.io/ingress.class")) }}
  {{- $_ := set .Values.s3www.ingress.annotations "kubernetes.io/ingress.class" .Values.s3www.ingress.className}}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-s3www
  labels:
    {{- include "s3www-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: s3www
  {{- with .Values.s3www.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.s3www.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.s3www.ingress.className }}
  {{- end }}
  {{- if .Values.s3www.ingress.tls }}
  tls:
    {{- range .Values.s3www.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.s3www.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-s3www
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}-s3www
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}